import { ReactNode } from "react";

export const PlayerPoster = (): ReactNode => (
    <div className="col-span-3 aspect-video rounded-md border bg-black text-card-foreground shadow flex items-center justify-center">
        <span className="leading-7">
            Paste a magnet above
        </span>
    </div>
);
